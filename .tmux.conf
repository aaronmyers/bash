#unbind C-b
#set -g prefix F3
#bind F3 send prefix
#
#set -g status-bg cyan

set -g base-index 1
set -g pane-base-index 1
#set-window-option -g automatic-rename off

set -g set-titles on
set -g set-titles-string "#I:#W"

set -g mouse on

set-option -g history-limit 5000

set -g renumber-windows on

bind | split-window -hc "#{pane_current_path}"
bind - split-window -vc "#{pane_current_path}"


bind -r "<" swap-window -d -t -1
bind -r ">" swap-window -d -t +1

bind c new-window -c "#{pane_current_path}"

bind Space last-window

bind-key -r -T prefix       M-Up              resize-pane -U 5
bind-key -r -T prefix       M-Down            resize-pane -D 5
bind-key -r -T prefix       M-Left            resize-pane -L 5
bind-key -r -T prefix       M-Right           resize-pane -R 5
bind-key -r -T prefix       C-Up              resize-pane -U
bind-key -r -T prefix       C-Down            resize-pane -D
bind-key -r -T prefix       C-Left            resize-pane -L
bind-key -r -T prefix       C-Right           resize-pane -R


# Style



#set-option -g status 2 
#set-option -g status-format[0] "top" 
#set-option -g status-format[1] "bottom"

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
#set -g @plugin 'tmux-plugins/tmux-sensible'

set -g @plugin 'catppuccin/tmux'
# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'github_username/plugin_name#branch'
# set -g @plugin 'git@github.com:user/plugin'
# set -g @plugin 'git@bitbucket.com:user/plugin'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'


set-option -g status 2
set -g status-format[1] '#[align=left] #(gitmux "#{pane_current_path}")'
#set -g status-right '#{prefix_highlight} | %a %Y-%m-%d %H:%M'
#set -g status-format[1] '#[align=left] #{prefix_highlight} | %a %Y-%m-%d %H:%M'
#set -g status-format[0] '#[align=left]Top left'
#set -ag status-format[0] '#[align=centre]Top center'
#set -ag status-format[0] '#[align=right]Top right'
#set -g status-format[1] '#[align=left]Bottom left'
#set -ag status-format[1] '#[align=centre]Bottom center'
#set -ag status-format[1] '#[align=right]Bottom right'
